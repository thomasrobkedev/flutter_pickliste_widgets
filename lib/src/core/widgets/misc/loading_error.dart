import 'package:flutter/material.dart';

class PicklisteLoadingError extends StatelessWidget {
  const PicklisteLoadingError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ERROR'),
    );
  }
}
